# DiscoAS UI 设计规范 (UI.md)

本文档记录 DiscoAS 的视觉设计要素，包括颜色、字体、布局等。

---

## 1. 颜色系统

### 1.1 品牌色

| 用途 | 颜色 | Hex |
|------|------|-----|
| 主色调 | 青色 | `#76E8FD` |
| 次要色 | 深青色 | `#1188DD` |

### 1.2 主题色（支持明暗切换）

#### 浅色模式 (Day)

| 元素 | 颜色 | Hex |
|------|------|-----|
| 卡片背景 | 浅灰白 | `#F0F0F0` |
| 卡片悬停 | 浅青色 | `#D0EBF0` |
| 卡片边框 | 青色 | `#76E8FD` |
| 卡片文字 | 深灰 | `#333333` |
| 取消按钮背景 | 浅红灰 | `#D6533E` |
| 取消按钮悬停 | 深红 | `#B03D2A` |
| 设置背景 | 浅灰 | `#F5F5F5` |
| 设置文字 | 深灰 | `#000000` |

#### 深色模式 (Night)

| 元素 | 颜色 | Hex |
|------|------|-----|
| 卡片背景 | 深灰 | `#2D2D2D` |
| 卡片悬停 | 深青 | `#1A3A40` |
| 卡片边框 | 青色 | `#76E8FD` |
| 卡片文字 | 白色 | `#FFFFFF` |
| 取消按钮背景 | 暗红 | `#8B3A3A` |
| 取消按钮悬停 | 深红 | `#6B2A2A` |
| 设置背景 | 深灰 | `#1E1E1E` |
| 设置文字 | 白色 | `#FFFFFF` |

### 1.3 功能色

| 用途 | 颜色 | Hex |
|------|------|-----|
| 加载按钮 | 蓝色 | `#3E8ED6` |
| 删除按钮 | 红色 | `#D6533E` |
| 阴影 | 半透明黑 | `rgba(0, 0, 0, 0.3)` |

---

## 2. 字体系统

### 2.1 字体设置

| 用途 | 字体 | 大小 |
|------|------|------|
| 歌曲标题 | 系统默认 | 12pt |
| 艺术家名 | 系统默认 | 10pt |
| 按钮文字 | 系统默认 | 10pt |
| 设置标题 | 系统默认 | 14pt |
| 设置正文 | 系统默认 | 10pt |

### 2.2 特殊样式

- 托盘菜单"发现歌曲"：**粗体**

---

## 3. 组件规格

### 3.1 歌曲卡片 (SongCardWidget)

| 属性 | 值 |
|------|-----|
| 尺寸 | 约 200x250 px（自适应） |
| 圆角 | 8px |
| 阴影 | 4px 模糊，黑色 30% 透明度 |
| 布局 | 垂直：封面 → 标题 → 艺术家 |

### 3.2 关闭按钮

| 属性 | 值 |
|------|-----|
| 位置 | 右上角 |
| 样式 | 圆形，红色背景 |
| 尺寸 | 32x32 px |

### 3.3 设置窗口

| 属性 | 值 |
|------|-----|
| 布局 | 左侧导航 + 右侧内容 |
| 导航宽度 | 200px |
| 窗口最小尺寸 | 700x500 px |
| 样式 | 模态对话框 |

---

## 4. 动画效果

### 4.1 启动画面 (SplashScreen)

| 阶段 | 时长 | 效果 |
|------|------|------|
| 淡入 | 750ms | 从透明 → 完全显示 |
| 保持 | 500ms | 静态显示 |
| 淡出 | 750ms | 从完全显示 → 透明 |
| **总时长** | **2000ms** | |

- 缓动曲线：`QEasingCurve.InOutQuad`
- 窗口属性：无边框、置顶、任务栏隐藏（Tool）

### 4.2 歌曲卡片悬停

- 背景色过渡到悬停色
- 过渡时长：约 200ms

### 4.3 发现界面打开/关闭动画 (DiscoverOverlay)

| 阶段 | 效果 | 时长 | 缓动曲线 |
|------|------|------|----------|
| 打开-位置 | y+40 → y | 380ms | OutBack |
| 打开-透明度 | 0 → 1 | 320ms | OutCubic |
| 关闭-位置 | y → y+20 | 220ms | InCubic |
| 关闭-透明度 | 1 → 0 | 220ms | InCubic |

- 窗口属性：无边框 (`FramelessWindowHint`)、置顶 (`WindowStaysOnTopHint`)、透明背景 (`WA_TranslucentBackground`)

### 4.4 Mask 遮罩动画（方案待定）

- `src/dark_mask.png` 带透明通道，用于视觉上突出卡片区域
- 动画方案：作为 DiscoverOverlay 的 child widget，透明度独立动画（打开时 fade_in，关闭时 fade_out）

---

## 5. 布局规范

### 5.1 主浮窗 (DiscoverOverlay)

- 类型：全屏透明浮窗 (`FramelessWindowHint`)
- 背景：完全透明 (`WA_TranslucentBackground`)
- 卡片网格：每行 5 张，自适应列数

### 5.2 设置页面（样例）

```
┌────────────┬────────────────────────────┐
│  导航栏     │      内容区域               │
│  ────────  │                            │
│  关于       │   [当前选中页面内容]        │
│  通用设置   │                            │
│            │                            │
│            │                            │
│            │                            │
│            │                            │
│  ────────  │                            │
│  保存/取消  │                            │
└────────────┴────────────────────────────┘
```

---

## 6. 图标资源

| 资源 | 路径 | 用途 |
|------|------|------|
| 应用图标 | `src/Icon.ico` | 系统托盘 |
| 启动画面 | `src/DiscoAS.png` | 启动动画 |
| 遮罩图 | `src/dark_mask.png` | 卡片区域视觉突出（带透明通道） |

---

## 7. 响应式规则

- 最大根据屏幕空间自动调整

---

## 8. 后续扩展

- [ ] 更多主题色预设
- [ ] 动态主题色（跟随系统）
